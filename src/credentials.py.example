# This is an example credentials file.
# For local development, copy this file to `credentials.py` and fill in your actual details.
# IMPORTANT: `credentials.py` is listed in .gitignore and will NOT be committed.

LINKEDIN_EMAIL = "your_email@example.com"
LINKEDIN_PASSWORD = "your_password"
