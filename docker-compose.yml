services:
  worker:
    build: .
    command: ["python", "-m", "src.worker"]
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  web:
    build: .
    command: gunicorn -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:5000 src.main:app
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
